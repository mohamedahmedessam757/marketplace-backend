import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { try { await prisma.$executeRawUnsafe('ALTER TABLE "stores" ADD COLUMN "logo" text;'); console.log('Added logo column'); } catch (e) { console.error('Error adding logo:', e); } try { await prisma.$executeRawUnsafe('ALTER TABLE "stores" ADD COLUMN "category" text;'); console.log('Added category column'); } catch (e) { console.error('Error adding category:', e); } try { await prisma.$executeRawUnsafe('ALTER TABLE "stores" ADD COLUMN "description" text;'); console.log('Added description column'); } catch (e) { console.error('Error adding description:', e); } } main().finally(() => prisma.$disconnect());
